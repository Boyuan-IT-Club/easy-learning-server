spring:
  datasource:
    url: jdbc:mysql://localhost:3306/early_learning?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
aliyun:
  flyway:
    enabled: true
    locations: classpath:db/migration
